const fullStack = [
    ['HTML', 'CSS', 'JS', 'React'],
    ['Node', 'Express', 'MongoDB']
];

for (let column = 0; column < fullStack.length; column++) {
    for (let row = 0; row < fullStack[column].length; row++) {
        console.log(fullStack[column][row]);
    }
}